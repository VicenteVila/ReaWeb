MEJORA ESTÉTICA del grafo de conocimientos actual en workspace/current (MUTA el candidato, no regeneres desde cero). Es una landing personal centrada en un GRAFO DE CONOCIMIENTOS interactivo (SVG puro, no canvas) con nodo raíz Vicente Vila (vicentevilaramirez@gmail.com) y los 6 repositorios de github.com/VicenteVila: TraceForge, CogniTeam, PromptForge, Asubarnipal, PopeBot-agente y ReaWeb. Cada repo, al hacer hover, despliega 2 subnodos: 'Conocimientos IA' (categorías sujet arXiv: cs.AI, cs.LG, cs.CL, cs.MA, cs.CV...) y 'README' (enlace a repos/<repo>/index.html). El nodo ReaWeb muestra SIEMPRE (sin hover) 3 subnodos de documentación que enlazan a las páginas locales repos/ReaWeb/docs/EVOLUTION/index.html, repos/ReaWeb/docs/READAPTATION/index.html y repos/ReaWeb/docs/REASONING/index.html (ya generadas en workspace/current/repos/). CONSERVA esos subnodos (docs visibles + hover de Conocimientos IA y README) y toda la estructura actual; solo mejora lo que un crítico de diseño VLM detectó como fallos:

CORRECCIONES EXIGIDAS:
1. DIBUJA ARISTAS VISIBLES entre el nodo raíz y cada repo, y entre ReaWeb y sus 3 docs (la estructura de grafo debe leerse, no nodos flotando). Añade <line> o <path> en el SVG con stroke que se ilumine/engrose en hover.
2. ENLAZA los subnodos: cada subnodo 'README' debe ser un <a> navegable a repos/<repo>/index.html, y cada subnodo de docs (EVOLUTION, READAPTATION, REASONING) un <a> a repos/ReaWeb/docs/<name>/index.html. Nada de solo texto.
3. AUMENTA LA LEGIBILIDAD: tipografía dentro de los nodos mayor (texto que se lea sin zoom), contraste suficiente (blanco puro o negro según el fondo del nodo, peso 'bold'), texto que no se corte. Usa <tspan> para etiquetas largas ('Conocimientos IA'). Escala el tamaño del nodo según la longitud del texto para que no se vea apretado.
4. DISTRIBUYE el grafo equilibradamente en el viewport (que ocupe el espacio, sin todo amontonado en el centro ni áreas vacías grandes). Reduce el espaciado vertical entre el grafo y la leyenda para cohesionar la composición.
5. FEEDBACK DE HOVER perceptible: glow (drop-shadow), escala del nodo, cambio de stroke, transición suave; el cursor debe invitar a explorar.
6. JERARQUÍA VISUAL: el nodo raíz 'Vicente Vila' debe destacar sobre los nodos secundarios (mayor tamaño, color distintivo, sombra paralela suave que separe el grafo del fondo).

REQUISITOS DE DISEÑO (no los rompas): SVG puro (sin canvas), dark/light con prefers-color-scheme y localStorage, animaciones reducidas si prefers-reduced-motion, accesible por teclado, sin dependencias externas ni CDN. Incluye leyenda (raíz, repo, conocimientos IA, README, docs de ReaWeb) y footer con nombre, email y enlace a github.com/VicenteVila. CONSERVA EN EL index.html RAÍZ la sección estática <section id="topics"> con las categorías arXiv (cs.AI, cs.LG, cs.CL, cs.MA, cs.CV, cs.SE, cs.IR, cs.DC): el evaluador exige esas keywords en el HTML raíz, no solo en el SVG dinámico de app.js; si la eliminas, el candidato se capa a 40. Después de generar, usa audit_visual para verificar que los fallos estéticos se resolvieron.

CORRECCIONES ESTRUCTURALES FINALES (ya validadas manualmente en el grafo actual; repítelas si regen era):
7. El subnodo 'Conocimientos IA' DEBE mostrar la información de los campos IA: al hacer hover sobre él se abre un panel lateral (aside #details-panel con #panel-title y #topics-list) con los códigos cs.XX + descripción de los topics de ESE repo (datos en app.js en un objeto 'topicData' embebido, igual que repos/ReaWeb/docs/...). Los subnodos se posicionan a -95,-75 (Conocimientos IA), 95,-75 (README) y 0,110 (Documentación) respecto al repo.
8. Los 3 subnodos de Documentación de ReaWeb se despliegan hacia la DERECHA del subnodo Documentación (offset +120 en x, con dy -100/0/+100): NUNCA a la izquierda (solapaban el grafo). Al hacer hover en el subnodo Documentación se muestran; al mover el ratón hacia ellos NO deben desaparecer (usa un retardo de ~180ms al ocultar en el mouseleave, cancelable si el ratón vuelve a entrar).
9. Casing uniforme en Title Case: 'Documentación', 'Evolución', 'Readaptación', 'Razonamiento' (los hrefs siguen apuntando a los directorios reales en mayúsculas repos/ReaWeb/docs/EVOLUTION/index.html, etc.).
10. El texto DEBE caber dentro de cada círculo: calcula el radio según la longitud de la palabra más larga del label (p.ej. radio = max(base, ceil(ancho/2)+6), ancho ≈ chars*fontSize*0.62). Con ello 'PopeBot-agente'≈59, 'Conocimientos IA'/'Documentación'≈51, 'Readaptación'≈47. Los subnodos usan font-size 11 y los repos/root 12.